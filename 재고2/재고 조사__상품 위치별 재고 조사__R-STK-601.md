# 페이지 분석 — 상품 위치별 재고 조사 (R-STK-601)

## 1. 페이지 개요

| 항목 | 내용 |
|------|------|
| 메뉴 그룹/위치 | 재고 조사 |
| 페이지명 | 상품 위치별 재고 조사 |
| 식별 코드 | R-STK-601 |
| 목적 | 창고 내 특정 위치(로케이션)에 보관된 상품의 전산재고와 실재고를 비교하여 차이를 확인하고, 재고조정 요청으로 연계하는 메뉴 |
| 접근 권한 | 물류사 작업자, 물류사 관리자, 시스템 관리자 |

---

## 2. 업무 흐름 (Business Flow)

```
[재고조사 시작]
    ↓
창고·로케이션 선택 (검색 필터)
    ↓
전산재고 목록 로드 (시스템 재고 수량 표시)
    ↓
작업자가 실재고 수량 직접 입력
    ↓
[저장] → 차이수량 자동 산출 (전산재고 - 실재고)
    ↓
차이 발생 항목 확인
    ↓
[재고조정 요청] 버튼 → R-STK-606 (재고조정) 연계
    ↓
관리자 승인 → 전산재고 업데이트
```

> **패션 WMS 특이사항**: 동일 스타일(스타일코드)이라도 색상/사이즈별로 개별 SKU 단위로 관리되므로, 컬러/사이즈 조합이 많을 경우 목록 건수가 상당히 많을 수 있다. 로케이션별 필터링을 필수로 사용하도록 UX를 유도해야 한다.

---

## 3. 화면 구성

### 3-1. 검색 필터

| 필드명 | 타입 | 필수 | 설명 |
|--------|------|------|------|
| 창고 | Select (단일) | ✅ | 조사 대상 창고 선택. 사용자 권한에 따라 접근 가능한 창고 목록만 표시 |
| 존(Zone) | Select (다중) | - | 창고 내 존 단위 필터 (예: A존, B존, 냉장존 등) |
| 로케이션 | Input + 자동완성 | - | 로케이션 코드 직접 입력 또는 범위 조회 (예: A-01-01 ~ A-01-10) |
| 화주 | Select (단일) | ✅ | 재고 귀속 화주 선택 |
| 브랜드 | Select (다중) | - | 화주 선택 후 연동. 패션 브랜드 단위 필터 |
| 상품코드 | Input | - | 내부 상품코드 (다중 입력: 콤마 구분 또는 줄바꿈) |
| 바코드 | Input | - | EAN-13 또는 내부 바코드. 스캔 입력 지원 |
| 스타일코드 | Input | - | 패션 스타일 단위 조회 (전체 색상·사이즈 포함) |
| 색상 | Select (다중) | - | 스타일코드 선택 후 연동 |
| 사이즈 | Select (다중) | - | S/M/L/XL/XXL 또는 숫자 사이즈 |
| 조사 차수 | Select | - | 진행 중인 재고조사 회차(차수) 선택. 기본값: 최신 차수 |
| 입력 상태 | Select | - | 전체 / 미입력 / 입력완료 (조사 진행률 확인용) |

**[검색] [초기화] 버튼**

---

### 3-2. 목록 컬럼

| 컬럼명 | 설명 | 수정 가능 |
|--------|------|----------|
| 창고 | 창고명 | ✗ |
| 로케이션 | 로케이션 코드 (예: A-02-03) | ✗ |
| 화주 | 화주명 | ✗ |
| 브랜드 | 패션 브랜드명 | ✗ |
| 상품코드 | 내부 SKU 코드 | ✗ |
| 바코드 | EAN-13 바코드 | ✗ |
| 스타일코드 | 스타일 단위 코드 | ✗ |
| 색상 | 색상명/색상코드 | ✗ |
| 사이즈 | 사이즈 표기 | ✗ |
| 상품명 | 상품 풀네임 | ✗ |
| 전산재고 | 시스템 기준 현재고 수량 | ✗ |
| 실재고 | 작업자가 직접 입력하는 실사 수량 | ✅ (인라인 편집) |
| 차이수량 | 전산재고 - 실재고 (자동 계산) | ✗ |
| 차이율(%) | (차이수량 / 전산재고) × 100 | ✗ |
| 입력상태 | 미입력 / 입력완료 (뱃지 표시) | ✗ |
| 입력자 | 실재고 입력한 작업자 이름 | ✗ |
| 입력일시 | 실재고 입력 일시 | ✗ |
| 비고 | 자유 메모 입력 (예: 불량, 위치이동 등) | ✅ |

> **차이수량 표시**: 양수(+) → 전산재고 과잉(파란색 표시), 음수(-) → 전산재고 부족(빨간색 표시), 0 → 일치(초록색 표시)

---

### 3-3. 요약 정보 (목록 상단)

| 항목 | 설명 |
|------|------|
| 총 조사 대상 SKU | 필터 조건 내 전체 건수 |
| 입력 완료 | 실재고 입력이 된 건수 |
| 미입력 | 아직 입력되지 않은 건수 |
| 진행률 | 입력완료 / 총 대상 (%) |
| 차이 발생 SKU | 전산재고와 실재고가 다른 건수 |

---

### 3-4. 액션 버튼

| 버튼명 | 권한 | 동작 |
|--------|------|------|
| 저장 | 작업자 이상 | 입력된 실재고 수량 일괄 저장 (변경된 행만 저장) |
| 재고조정 요청 | 작업자 이상 | 차이수량 발생 항목 선택 후 R-STK-606으로 조정 요청 생성 |
| 엑셀 다운로드 | 전체 | 현재 목록 전체 엑셀 다운로드 |
| 엑셀 업로드 | 작업자 이상 | 실재고 수량을 엑셀로 일괄 입력 (서식 다운로드 제공) |
| 초기화 | 관리자 이상 | 선택한 항목의 실재고 입력값 초기화 (확인 팝업 필수) |
| 인쇄 | 전체 | 재고조사표 출력 (로케이션별 체크리스트 형식) |

---

## 4. 상태값 및 전이

```
미입력 (gray) ──→ 입력중 (orange) ──→ 입력완료 (green)
                                           ↓
                                   [차이 발생 시]
                                   재고조정 요청됨 (blue)
                                           ↓
                                   R-STK-606 승인 완료 (purple)
```

| 상태값 | 설명 |
|--------|------|
| 미입력 | 실재고 수량이 입력되지 않은 상태 |
| 입력완료 | 실재고 수량이 저장된 상태 |
| 재고조정 요청됨 | 해당 SKU에 대해 재고조정(R-STK-606) 요청이 생성된 상태 |
| 조정완료 | 재고조정 승인이 완료되어 전산재고가 업데이트된 상태 |

---

## 5. 입력 검증 규칙

| 필드 | 규칙 |
|------|------|
| 실재고 | 0 이상의 정수만 허용. 소수점 입력 불가 |
| 실재고 | 최대값 제한: 9,999 (초과 시 경고 팝업 — 의도적 대량 입력 확인) |
| 실재고 | 빈값 저장 불가 (저장 시 미입력 상태 유지) |
| 비고 | 최대 200자 |

**저장 전 확인 팝업**: "총 N건의 실재고가 저장됩니다. 계속하시겠습니까?"

---

## 6. 권한 매트릭스

| 기능 | 화주 | 물류사 작업자 | 물류사 관리자 | 시스템 관리자 |
|------|:----:|:------------:|:------------:|:------------:|
| 페이지 접근 | ✗ | ✅ | ✅ | ✅ |
| 실재고 입력/저장 | ✗ | ✅ | ✅ | ✅ |
| 재고조정 요청 | ✗ | ✅ | ✅ | ✅ |
| 입력값 초기화 | ✗ | ✗ | ✅ | ✅ |
| 엑셀 다운로드 | ✗ | ✅ | ✅ | ✅ |
| 엑셀 업로드 | ✗ | ✅ | ✅ | ✅ |

---

## 7. API/이벤트

| API | Method | 설명 |
|-----|--------|------|
| `/api/stock/inventory-count/list` | GET | 필터 조건에 따른 재고조사 목록 조회 |
| `/api/stock/inventory-count/save` | POST | 실재고 수량 일괄 저장 |
| `/api/stock/inventory-count/request-adjustment` | POST | 선택 항목 재고조정 요청 생성 (R-STK-606 연계) |
| `/api/stock/inventory-count/excel-download` | GET | 목록 엑셀 다운로드 |
| `/api/stock/inventory-count/excel-upload` | POST | 실재고 수량 엑셀 일괄 업로드 |
| `/api/stock/inventory-count/print-template` | GET | 재고조사표 인쇄용 데이터 |

---

## 8. 엑셀 업로드 서식 (실재고 일괄 입력)

| 컬럼 | 필수 | 설명 |
|------|------|------|
| 로케이션코드 | ✅ | 시스템 로케이션 코드와 일치해야 함 |
| 바코드 또는 상품코드 | ✅ | 둘 중 하나 필수 |
| 실재고수량 | ✅ | 0 이상 정수 |
| 비고 | - | 자유 메모 |

**업로드 처리 결과**: 성공 건수 / 실패 건수 / 실패 사유 목록 팝업으로 표시

---

## 9. 연관 페이지 및 흐름

| 연관 페이지 | 방향 | 설명 |
|------------|------|------|
| R-STK-602 (상품 가입고) | 선행 | 가입고 수량을 기반으로 재고조사를 진행하는 흐름 |
| R-STK-605 (가입고 현재고 대조) | 연계 | 가입고 수량과 현재고 차이 확인 후 이 화면에서 실사 진행 |
| R-STK-606 (재고조정) | 후행 | 차이 발생 시 재고조정 요청 생성 |
| R-STK-1001 (위치별 재고 현황) | 참고 | 로케이션별 현재 가용재고 확인 |

---

## 10. 예외 처리 및 알림

| 상황 | 처리 방식 |
|------|----------|
| 전산재고 = 0인데 실재고 > 0 입력 | 경고 메시지: "전산재고가 0인 항목입니다. 실재고를 입력하시겠습니까?" (확인/취소) |
| 이미 재고조정 요청된 항목 재입력 | "이미 조정 요청된 항목입니다. 기존 요청을 취소 후 재입력하세요." |
| 진행 중인 재고조사 차수 없음 | "진행 중인 재고조사 차수가 없습니다. 관리자에게 문의하세요." |
| 동일 SKU 중복 로케이션 존재 | 로케이션별로 구분하여 개별 행으로 표시 |
| 저장 중 네트워크 오류 | 자동 재시도(3회) 후 실패 시 토스트 에러 메시지 |
