# 페이지 분석 — 가입고 현재고 대조 (R-STK-605)

## 1. 페이지 개요

| 항목 | 내용 |
|------|------|
| 메뉴 그룹/위치 | 재고 조사 |
| 페이지명 | 가입고 현재고 대조 |
| 식별 코드 | R-STK-605 |
| 목적 | 재고조사 과정에서 입력된 가입고 수량과 시스템의 현재고(전산재고) 수량을 SKU 단위로 비교하여, 차이 발생 항목을 식별하고 재고조정(R-STK-606)으로 연계하는 메뉴 |
| 접근 권한 | 물류사 관리자, 시스템 관리자 (작업자는 읽기 전용 접근 가능) |

> **R-STK-604와의 차이점**: R-STK-604는 "가입고 수량 vs 실입고 처리 수량" 비교. R-STK-605는 "가입고 수량(실사 결과) vs 현재 전산재고 수량" 비교로, 실질적인 재고 조정 필요 여부를 판단하는 데 사용한다.

---

## 2. 업무 흐름 (Business Flow)

```
[가입고 등록 완료] (R-STK-602)
    ↓
[가입고 현재고 대조] (R-STK-605) 진입
    ↓
조사 차수·화주사·창고 설정 후 [검색]
    ↓
가입고수량(실사) vs 현재고(전산) 비교 결과 표시
    ↓
차이 항목 분석
    ├── 차이 없음 → 재고 정확도 양호, 조사 완료
    ├── 가입고 > 현재고 → 현재고 부족. 재고조정(+) 필요
    └── 가입고 < 현재고 → 현재고 과잉. 재고조정(-) 필요
    ↓
[재고조정 요청] → R-STK-606 (작업자 요청 → 관리자 승인)
    ↓
승인 완료 → 전산재고 업데이트
```

---

## 3. 화면 구성

### 3-1. 검색 필터

| 필드명 | 타입 | 필수 | 설명 |
|--------|------|------|------|
| 화주사 | Select | ✅ | 조회 대상 화주사 |
| 창고 | Select | ✅ | 조사 대상 창고 |
| 조사 차수 | Select | ✅ | 재고조사 회차. 기본값: 최신 활성 차수 |
| 대조 기준일 | Date | - | 현재고 스냅샷 기준일 (기본값: 오늘). 특정 시점의 재고로 비교 가능 |
| 로케이션 | Input | - | 특정 로케이션 한정 조회 |
| 브랜드 | Select (다중) | - | 패션 브랜드 필터 |
| 상품코드 | Input | - | SKU 코드 |
| 바코드 | Input | - | 바코드 스캔 또는 직접 입력 |
| 스타일코드 | Input | - | 스타일 단위 조회 |
| 색상 | Select (다중) | - | 색상 필터 |
| 사이즈 | Select (다중) | - | 사이즈 필터 |
| 대조 결과 | Select | - | 전체 / 일치 / 가입고 초과 / 현재고 초과 / 가입고만 존재 / 현재고만 존재 |
| 차이수량 임계값 | Input | - | 차이수량 N 이상인 항목만 조회 (예: 5 이상 차이 발생 건만) |

**[검색] [초기화] 버튼**

---

### 3-2. 요약 카드 (상단)

| 항목 | 설명 |
|------|------|
| 총 SKU 건수 | 조회 조건 내 전체 SKU 수 |
| 일치 건수 | 가입고 = 현재고인 SKU 수 |
| 불일치 건수 | 가입고 ≠ 현재고인 SKU 수 |
| 가입고 초과 건수 | 가입고 > 현재고 (재고 증가 조정 필요) |
| 현재고 초과 건수 | 가입고 < 현재고 (재고 감소 조정 필요) |
| 총 차이 수량 (절대값 합계) | 불일치 SKU들의 차이수량 절대값 합산 |
| 재고 정확도 | 일치 건수 / 총 SKU 건수 × 100 (%) |

---

### 3-3. 목록 컬럼

| 컬럼명 | 설명 |
|--------|------|
| 화주사 | 화주사명 |
| 창고 | 창고명 |
| 로케이션 | 로케이션 코드 |
| 브랜드 | 패션 브랜드명 |
| 상품코드 | SKU 코드 |
| 바코드 | EAN-13 바코드 |
| 스타일코드 | 스타일 코드 |
| 색상 | 색상명 |
| 사이즈 | 사이즈 표기 |
| 상품명 | 상품 풀네임 |
| 가입고수량 | 재고조사에서 실사된 수량 (해당 차수 합계) |
| 현재고수량 | 시스템 전산재고 수량 (대조 기준일 기준) |
| 차이수량 | 가입고수량 - 현재고수량 |
| 차이율(%) | (차이수량 / 현재고수량) × 100 |
| 대조 결과 | 일치 / 가입고 초과 / 현재고 초과 (뱃지 색상 구분) |
| 재고조정 상태 | 미요청 / 조정 요청됨 / 승인완료 |

> **패션 WMS 특이사항**: 시즌 전환기(SS/FW)에 재고조사를 진행하면 수천~수만 SKU의 대조 결과가 발생한다. "재고 정확도" KPI를 화면에 표시하여 운영 품질을 모니터링하는 것이 중요하다.

---

### 3-4. 액션 버튼

| 버튼명 | 권한 | 동작 |
|--------|------|------|
| 재고조정 요청 | 관리자 이상 | 선택된 불일치 항목에 대해 R-STK-606 재고조정 요청 일괄 생성 |
| 엑셀 다운로드 | 전체 | 현재 대조 결과 목록 엑셀 다운로드 |
| 상세 내역 보기 | 전체 | 선택 SKU의 가입고 상세 건별 목록 팝업 |
| 현재고 새로고침 | 관리자 이상 | 현재고 수량을 최신 값으로 다시 불러오기 |

---

## 4. 대조 결과 정의

| 결과값 | 조건 | 의미 | 색상 |
|--------|------|------|------|
| 일치 | 가입고 = 현재고 | 전산재고와 실재고 일치. 조정 불필요 | Green |
| 가입고 초과 | 가입고 > 현재고 | 실재고가 전산보다 많음. 전산재고 증가 조정 필요 | Blue |
| 현재고 초과 | 가입고 < 현재고 | 전산재고가 실재고보다 많음. 전산재고 감소 조정 필요 | Red |
| 가입고만 존재 | 현재고 = 0, 가입고 > 0 | 전산에 없던 재고가 현장에서 발견됨 | Purple |
| 현재고만 존재 | 가입고 = 0, 현재고 > 0 | 전산재고는 있으나 현장에서 재고 미발견 | Orange |

---

## 5. 재고조정 일괄 요청 흐름

불일치 항목 선택 → [재고조정 요청] 버튼 클릭

팝업에서 조정 사유 입력 (공통 사유 적용 또는 건별 입력):
- 재고조사 실사 결과에 의한 조정
- 파손/불량 처리
- 분실
- 기타 (직접 입력)

확인 → R-STK-606 재고조정 요청 일괄 생성 → 관리자 승인 대기

---

## 6. 권한 매트릭스

| 기능 | 화주사 | 물류사 작업자 | 물류사 관리자 | 시스템 관리자 |
|------|:----:|:------------:|:------------:|:------------:|
| 페이지 접근 | ✗ | ✅ (읽기전용) | ✅ | ✅ |
| 조회/검색 | ✗ | ✅ | ✅ | ✅ |
| 재고조정 요청 | ✗ | ✗ | ✅ | ✅ |
| 엑셀 다운로드 | ✗ | ✅ | ✅ | ✅ |

---

## 7. API/이벤트

| API | Method | 설명 |
|-----|--------|------|
| `/api/stock/pre-receipt-vs-stock/list` | GET | 가입고 현재고 대조 목록 조회 |
| `/api/stock/pre-receipt-vs-stock/summary` | GET | 요약 카드 집계 데이터 (재고 정확도 포함) |
| `/api/stock/pre-receipt-vs-stock/request-adjustment` | POST | 선택 항목 재고조정 일괄 요청 |
| `/api/stock/pre-receipt-vs-stock/excel-download` | GET | 대조 결과 엑셀 다운로드 |

---

## 8. 연관 페이지 및 흐름

| 연관 페이지 | 방향 | 설명 |
|------------|------|------|
| R-STK-601 (상품 위치별 재고 조사) | 선행 | 로케이션별 실사 결과 입력 |
| R-STK-602 (상품 가입고) | 선행 | 가입고 데이터 원천 |
| R-STK-604 (가입고 실입고 대조) | 연계 | 가입고 vs 실입고 비교 (다른 관점) |
| R-STK-606 (재고조정) | 후행 | 불일치 항목 재고조정 요청 처리 |

---

## 9. 예외 처리 및 알림

| 상황 | 처리 방식 |
|------|----------|
| 진행 중인 차수 없음 | "선택한 조건에 해당하는 재고조사 차수가 없습니다." |
| 이미 재고조정 요청된 항목 재요청 | "이미 조정 요청된 항목이 포함되어 있습니다. 기존 요청을 취소 후 다시 요청하세요." |
| 대조 기준일이 미래 날짜 | "대조 기준일은 오늘 이전 날짜만 선택 가능합니다." 검증 에러 |
| 가입고 수량 = 현재고 수량인 항목 조정 요청 시도 | "선택된 항목 중 차이가 없는 항목은 재고조정 요청 대상에서 제외됩니다." 안내 |
